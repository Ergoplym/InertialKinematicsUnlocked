numpy>=1.24
scipy>=1.10
matplotlib>=3.7
pandas>=2.0
PyQt5>=5.15
opencv-python>=4.8

# NOTE: Xsens Awinda / MTw support requires the Xsens Device API Python bindings
# (xsensdeviceapi). This is provided by Xsens (Movella) SDK, not via pip.
